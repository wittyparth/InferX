# Production Environment Variables
# Copy this to .env.prod and fill in real values

# ========== Database Configuration ==========
DB_USER=mluser
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
DATABASE_URL=postgresql://mluser:CHANGE_ME_TO_STRONG_PASSWORD@postgres:5432/ml_platform

# ========== Redis Configuration ==========
REDIS_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
REDIS_URL=redis://:CHANGE_ME_TO_STRONG_PASSWORD@redis:6379

# ========== API Configuration ==========
API_V1_PREFIX=/api/v1
PROJECT_NAME=ML Model Serving Platform
VERSION=1.0.0

# ========== Security ==========
# Generate a strong secret key: openssl rand -base64 32
SECRET_KEY=CHANGE_ME_TO_STRONG_SECRET_KEY
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ========== CORS Configuration ==========
# Adjust for your actual domain
BACKEND_CORS_ORIGINS=["https://yourdomain.com", "https://www.yourdomain.com"]

# ========== Frontend Configuration ==========
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
NODE_ENV=production

# ========== SSL/TLS (if using Nginx) ==========
# SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
# SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# ========== Monitoring & Logging ==========
LOG_LEVEL=info
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ========== Email Configuration (if needed) ==========
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# ========== AWS S3 (if using for model storage) ==========
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1
